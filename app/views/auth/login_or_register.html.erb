<% if alert %>
  <div class="alert"><%= alert %></div>
<% end %>

<%= form_with( url: '/auth/create_session', method: :post) do |form| %>
  <h2>Login</h2>

  <div class="field">
    <%= form.label :email_address %>
    <%= form.text_field :email_address, name: 'auth[email]' %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, name: 'auth[password]' %>
  </div>

  <div class="actions">
    <%= form.submit "Login", name: :login %>
  </div>
<% end %>


<%= form_with(url: '/users/create', method: :post) do |form| %>
  <h2>Register</h2>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, name: 'user[username]' %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, name: 'user[name]' %>
  </div>

  <div class="field">
    <%= form.label :surname %>
    <%= form.text_field :surname, name: 'user[surname]' %>
  </div>

  <div class="field">
    <%= form.label :birthdate %>
    <%= form.date_field :birthdate, name: 'user[birthdate]' %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number, name: 'user[phone_number]' %>
  </div>

  <div class="field">
    <%= form.label :email_address %>
    <%= form.text_field :email_address, name: 'user[email_address]' %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password, name: 'user[password]' %>
  </div>

  <div class="actions">
    <%= form.submit "Register" %>
  </div>

<% end %>

<%= form_tag('/auth/developer', method: 'post', data: {turbo: false}) do %>
  <button type='submit'>Login with Developer</button>
<% end %>
<%= form_tag('/auth/facebook', method: 'post', data: {turbo: false}) do %>
  <button type='submit'>Login with Facebook</button>
<% end %>
<%= form_tag('/auth/google_oauth2', method: 'post', data: {turbo: false}) do %>
  <button type='submit'>Login with Google</button>
<% end %>
<%= form_tag('/auth/twitter', method: 'post', data: {turbo: false}) do %>
  <button type='submit'>Login with Twitter</button>
<% end %>
